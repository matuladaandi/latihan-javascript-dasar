// created class js version old
function Person() {
  this.firstName = "";
  this.lastName = "";
}

const and = new Person();
and.firstName = "andi";
and.lastName = "matulada";

const bud = new Person();
bud.firstName = "budi";

console.info(and);
console.info(bud);
